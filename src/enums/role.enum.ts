// rol.enum.ts

// Roles enum
// This enum contains the roles that can be assigned to a user
export enum Role {
    ADMIN = 'admin',
    PROFESOR_GUIA = 'profesor guia',
    ESTUDIANTE = 'estudiante',
    COORDINADOR = 'coordinador'
};
